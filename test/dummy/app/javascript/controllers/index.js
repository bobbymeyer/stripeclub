import { application } from "controllers/application"

// its-swiss registers its own controllers from the module its shell imports;
// a host registers nothing for it. The engine ships no JavaScript of its own.
